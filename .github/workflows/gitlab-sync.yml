name: GitLab Sync

on:
  push:
    branches:
      - main

jobs:
  trigger-github-push:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger GitHub push (optional - only needed if you want a notification)
        run: echo "GitLab push detected. GitHub mirror will update shortly."